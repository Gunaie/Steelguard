# Grafana 数据源自动预置(阶段5.2): Prometheus 容器内地址
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: PROMETHEUS
    url: http://prometheus:9090
    isDefault: true
    editable: true
